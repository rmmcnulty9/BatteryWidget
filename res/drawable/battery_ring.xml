<?xml version="1.0" encoding="utf-8"?>
<shape 
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="ring"
    android:innerRadius="@dimen/ring_radius"
    android:thickness="@dimen/ring_border_width"
    android:useLevel="false">
    

    <size
        android:width="@dimen/ring_width"
        android:height="@dimen/ring_height" />
    
    <stroke
        android:width="@dimen/ring_border_width"
        android:color="@color/ring_border_color" />
    
</shape>